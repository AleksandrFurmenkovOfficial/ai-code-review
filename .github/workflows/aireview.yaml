name: PR review

on: 
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

jobs:
  annotate-pr:
    runs-on: ubuntu-latest
    name: Add AI comment with review for changes
    steps:
      - name: AI review
        uses: AleksandrFurmenkovOfficial/ai-code-review@main
        with:
          token: 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
          openai_api_key: w1m3wK1xdOpCEFfS52NsT3BlbkFJ1VfIJo671eHHRA3nh59M
          owner: ${{ github.repository_owner }}
          repo: ${{ github.event.repository.name }}
          pr_number: ${{ github.event.number }}
          #file_extensions: ".py,.js,.html" # for example
          #exclude_paths: "test/,docs/"     # for example
